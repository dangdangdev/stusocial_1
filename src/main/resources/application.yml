spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stusocial?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#server:
 # port: 443
 # ssl:
   # key-store: classpath:3751191_www.miraclebay.site.pfx
   # key-store-password:
    #key-store-type: PKCS12
    #enabled: true

